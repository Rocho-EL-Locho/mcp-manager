{
  "name": "mcp-manager",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "GUI zur Verwaltung lokaler MCP-Server für Claude Code",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "APPIMAGE_EXTRACT_AND_RUN=1 NO_STRIP=1 tauri build"
  },
  "dependencies": {
    "@tauri-apps/api": "^2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "typescript": "^6.0.3",
    "vite": "^8.1.2"
  }
}
